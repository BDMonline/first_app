This is the update page. 

<%= provide(:title, 'New Question')%> <%= @error %>
<h1>Please correct the Question</h1>


<div class="row">
  <div class="span6 offset3">
    <%= form_for(@question) do |f| %>
      <%= render 'shared/qerror_messages' %>

      <%= f.label :name, :style => "font-size: 1.7em;"%>
      <%= f.text_field :name %>
      <h1><p></h1>

      <%= f.label :text, :style => "font-size: 1.7em;"%>  Use ` as delimiter for any embedded expressions
      <%= f.text_field :text %>
      Sample question text: <h7><%= @example_question %> </h7>
      <h1><p></h1>

      <%= f.label :parameters, :style => "font-size: 1.7em;"%> If parameters are needed, give names (uppercase letters) and ranges, separated by spaces, e.g. "A (2,3,7,8) B (-2..5) C [0..3]"
      <%= f.text_field :parameters %>
      Parameter choice:<h7> <%= @paramtext %> </h7> <p>
      Parameter assignments used for illustration purposes on this page:<h7> <%= @example_param_hash.to_s %> </h7> <p>
      <h1><p></h1>

      <%= f.label :answers, :style => "font-size: 1.7em;"%> Use ` to separate multiple answers. End with "t" if order matters, "f" if not.
      <%= f.text_field :answers %>
      Answers for example question: <h7><%= @example_answers %></h7>
      <h1><p></h1>

      <%= f.label :precision_regime, :style => "font-size: 1.7em;"%> 0h for exact answers only, Otherwise number of sf + h/s/r (harsh/soft/round)
      <%= f.text_field :precision_regime %>
      <h1><p></h1>

      <%= f.submit "Edit Question", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>